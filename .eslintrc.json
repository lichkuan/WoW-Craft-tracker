{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-console": ["warn", { "allow": ["warn", "error"] }],
    "react/no-unescaped-entities": "warn"
  },
  "overrides": [
    {
      "files": ["app/api/**/*.ts", "lib/logger.ts"],
      "rules": {
        "no-console": "off"
      }
    }
  ]
}